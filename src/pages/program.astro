---
import capitalize from "lodash/capitalize";
import groupBy from "lodash/groupBy";
import sortBy from "lodash/sortBy";
import BuyTicketsButton from "../components/BuyTicketsButton.astro";

import { Image } from "astro:assets";
import { getCollection } from "astro:content";
import classNames from "classnames";
import { isBefore, isValid } from "date-fns";
import BarnekonsertTag from "../components/BarnekonsertTag.astro";
import BestillingsverkTag from "../components/BestillingsverkTag.astro";
import FreeTag from "../components/FreeTag.astro";
import Link from "../components/Link.astro";
import MorgensoloTag from "../components/MorgensoloTag.astro";
import SoldOutTag from "../components/SoldOutTag.astro";
/* import programPlakat2026 from "../images/profil/2026/Mandaljazz_26_plakat_komplett_navn.jpg"; */
import Layout from "../layouts/Layout.astro";
import { getArtistUrl } from "../utils/artist";
import { formatHumanDate, formatTime } from "../utils/date";
import { imagePathToSrc } from "../utils/images";

const artistEntries = await getCollection("artist");
const artists: Array<{
  slug: string;
  frontmatter: (typeof artistEntries)[number]["data"];
}> = artistEntries
  .map((entry) => ({
    slug: entry.slug,
    frontmatter: entry.data,
  }))
  // Filter to only show current year artists (2026)
  .filter((artist) => {
    const currentYear = "2026";
    return (
      artist.slug.startsWith(`${currentYear}/`) || artist.slug === currentYear
    );
  });

const otherEvents = [];

const concertsGroupedByDay = Object.entries(
  groupBy(
    sortBy(
      [
        ...artists.filter(
          ({ frontmatter: { concertStartAt } }) =>
            concertStartAt && isValid(new Date(concertStartAt)),
        ),
        ...otherEvents.map((tbaArtist) => ({
          slug: "",
          frontmatter: tbaArtist,
        })),
      ],
      "frontmatter.concertStartAt",
    ),
    (artistPage) => formatHumanDate(artistPage.frontmatter.concertStartAt),
  ),
);
---

<Layout
  title="Program // Mandaljazz 2026"
  description="Programmet til Mandaljazz 2026"
>
  <main class="px-4 py-24 sm:px-12 sm:py-40 md:px-24 lg:px-32">
    <div class="mx-auto max-w-5xl space-y-16">
      <h1 class="text-5xl md:text-7xl">Program</h1>
      {
        concertsGroupedByDay.map(([day, artists]) => {
          return (
            <div class="space-y-4">
              <h2 class="sticky top-[52px] z-10 w-fit rounded-xl rounded-tl-none bg-blue p-4 text-2xl text-beige lg:top-[56px]">
                {capitalize(day)}
              </h2>
              <div class="grid w-full grid-cols-4 gap-y-4 text-lg">
                <div class="col-span-3 border-b border-black py-3 font-normal md:col-span-2 md:block">
                  Artist
                </div>
                <div class="hidden border-b border-black py-3 font-normal md:block">
                  Scene
                </div>
                <div class="border-b border-black py-3 text-right font-normal md:block">
                  Tid
                </div>
                {
                  // @ts-ignore - This is the list of artistPages grouped by day
                  artists.map(
                    ({
                      slug,
                      frontmatter: {
                        title,
                        shortTitle,
                        tagline,
                        image,
                        venue,
                        concertStartAt,
                        concertEndAt,
                        hideFromProgram,
                        isPlaceholder,
                        isMorgensolo,
                        isFree,
                        externalLink,
                        isBestillingsverk,
                        isBarnekonsert,
                        releaseAt,
                        cancelled,
                        soldOut,
                      },
                    }) => {
                      const isNotReleasedYet =
                        hideFromProgram ||
                        (releaseAt &&
                          isBefore(new Date(), new Date(releaseAt)));

                      if (isNotReleasedYet) {
                        return (
                          <div class="col-span-4 grid grid-cols-4 gap-x-2 gap-y-0.5">
                            <div class="col-span-4 flex flex-row items-center gap-2 text-xl md:col-span-2 md:text-lg">
                              <Image
                                src={imagePathToSrc(
                                  "/src/images/profil/logo/primary/black.svg",
                                )}
                                alt={"Kommer…"}
                                class="h-8 w-8 object-contain"
                              />
                              Kommer…
                            </div>
                            <div class="col-span-3 md:col-span-1">{venue}</div>
                            <div class="text-right">{`${formatTime(concertStartAt)}${concertEndAt ? `–${formatTime(concertEndAt)}` : ""}`}</div>
                          </div>
                        );
                      }

                      return (
                        <div class="col-span-4 grid grid-cols-4 gap-x-2 gap-y-0.5">
                          <div class="col-span-4 flex flex-row items-center gap-2 text-xl sm:col-span-3 md:col-span-2 md:text-lg">
                            {slug ? (
                              <img
                                src={image}
                                alt={shortTitle || title}
                                class="h-8 w-8 rounded-lg object-cover"
                              />
                            ) : (
                              <Image
                                src={imagePathToSrc(
                                  "/src/images/profil/logo/primary/black.svg",
                                )}
                                alt={shortTitle || title}
                                class="h-8 w-8 object-contain"
                              />
                            )}
                            <div>
                              <div class="flex flex-row items-center gap-2">
                                {slug && !isPlaceholder ? (
                                  <div>
                                    <Link
                                      href={getArtistUrl(slug)}
                                      title={title}
                                    >
                                      {/* prettier-ignore */}
                                      <span class={classNames({
                                          "line-through": cancelled,
                                        })}>{shortTitle || title}</span>
                                      {cancelled && (
                                        <span class="ml-1 text-sm">
                                          {" "}
                                          (AVLYST)
                                        </span>
                                      )}
                                    </Link>
                                  </div>
                                ) : externalLink ? (
                                  <div>
                                    <Link
                                      href={externalLink}
                                      external
                                      title={title}
                                    >
                                      {/* prettier-ignore */}
                                      <span class={classNames({
                                          "line-through": cancelled,
                                        })}>{title}</span>
                                      {cancelled && (
                                        <span class="ml-1 text-sm">
                                          {" "}
                                          (AVLYST)
                                        </span>
                                      )}
                                    </Link>
                                  </div>
                                ) : (
                                  <div title={title}>{title}</div>
                                )}
                                <div class="hidden flex-row items-start gap-2 sm:flex">
                                  {isMorgensolo && (
                                    <MorgensoloTag size={"small"} />
                                  )}
                                  {isBestillingsverk && (
                                    <BestillingsverkTag size={"small"} />
                                  )}
                                  {isBarnekonsert && (
                                    <BarnekonsertTag size={"small"} />
                                  )}
                                  {isFree && <FreeTag size={"small"} />}
                                  {soldOut && <SoldOutTag size={"small"} />}
                                </div>
                              </div>

                              <p class="hidden text-sm sm:block">{tagline}</p>
                            </div>
                          </div>
                          <div class="col-span-3 md:col-span-1">
                            <div class="flex flex-row items-start gap-2">
                              <span>{venue}</span>
                              <div class="flex flex-row items-start gap-2 sm:hidden">
                                {isMorgensolo && (
                                  <MorgensoloTag size={"small"} />
                                )}
                                {isBestillingsverk && (
                                  <BestillingsverkTag size={"small"} />
                                )}
                                {isBarnekonsert && (
                                  <BarnekonsertTag size={"small"} />
                                )}
                                {isFree && <FreeTag size={"small"} />}
                                {soldOut && <SoldOutTag size={"small"} />}
                              </div>
                            </div>
                          </div>
                          <div class="col-span-1 flex flex-col text-right">
                            <span>{formatTime(concertStartAt)}</span>
                            {concertEndAt && (
                              <span class="whitespace-nowrap text-xs">
                                (ferdig {formatTime(concertEndAt)})
                              </span>
                            )}
                          </div>
                        </div>
                      );
                    },
                  )
                }
              </div>
            </div>
          );
        })
      }
      <BuyTicketsButton class="mx-auto w-fit" />
      <!-- <Image
        src={programPlakat2026}
        alt="Mandaljazz program 2026"
        quality={100}
        width={720}
        class="mx-auto max-w-72 md:max-w-md"
      /> -->
    </div>
  </main>
</Layout>
