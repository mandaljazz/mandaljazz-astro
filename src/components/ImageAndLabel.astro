---
export interface Props {
  image: string;
  alt: string;
}

const { image, alt } = Astro.props;
---

<span class="flex flex-row gap-2 items-center text-lg"
  ><img src={image} alt={alt} class="w-12 h-12 rounded-full" /><slot /></span
>
