---
export interface Props {
  image: string;
}

const { image } = Astro.props;
---

<span class="flex flex-row gap-2 items-center text-lg"
  ><img src={image} class="w-12 h-12 rounded-full" /><slot /></span
>
