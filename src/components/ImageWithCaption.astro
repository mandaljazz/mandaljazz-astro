---
export interface Props {
  src: string;
  caption?: string;
}

const { src, caption } = Astro.props;
---

<figure>
  <img src={src} alt={caption} class="rounded-t-lg" />
  <figcaption class="p-4 bg-dark-blue/10 rounded-b-lg">
    Frivilliggjengen {caption}
  </figcaption>
</figure>
